# Claude Voice Session
**Date**: 2026-04-25
**Time**: 16:04:26

---

**[16:05:02] You**
I have another small UI change I want to make.

**[16:05:02] Claude**
Sure, what's the change?

---

**[16:05:27] You**
When you are using a tool, you list it out on the UI, which is good, but

**[16:05:27] Claude**
But it cuts off — what's the second part?

---

**[16:07:28] You**
When you're going through multiple iterations of tool then message, then tool, then message, all of the messages get grouped into a single block and the tool use displays below the message block, but that's not accurately depicting the order in which events occurred.

**[16:07:28] Claude**
That makes sense — the tools should appear interleaved with the messages in the order they actually happened, not all the tools lumped together at the bottom. Let me look at the UI code to understand how it's currently rendering things.Okay, I see the issue. Right now, tool results come in as their own bubbles via the `tool_result` message, but the Claude text accumulates in `currentClaudeBubble` which gets started whenever the first audio chunk arrives. So if there are multiple tool calls, all the text gets bundled into one bubble that ends up after all the tools.

To fix this properly, the server needs to tell the client when a new text segment starts — so each message chunk between tool calls gets its own bubble. Let me check the server-side code to understand what messages it sends.Now I have a clear picture. The fix needs two parts. On the server side, when we finish streaming one text segment and are about to run tools, we need to send a signal to the client saying "this text segment is done, start a new bubble next time." And on the client side, we need to respect that signal.

The cleanest way is to send a `new_bubble` message from the server right before executing tools, so the client knows to close off the current bubble and start fresh after the tools run. Let me make both changes.Now update the client to handle `close_bubble` and reset `currentClaudeBubble` so the next audio chunk starts a new bubble.Done. Now when I finish a text segment and am about to run tools, the server sends a `close_bubble` message. The client resets the current bubble, so any text that comes after the tools gets its own fresh bubble. The conversation will show text, then tools, then text, in the right order.

---


<!-- consolidated -->

<!-- consolidated -->
