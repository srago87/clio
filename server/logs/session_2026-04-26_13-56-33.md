# Claude Voice Session
**Date**: 2026-04-26
**Time**: 13:56:33

---

**[13:58:28] You**
We just gave you the ability to reestablish a client connection if the web socket drops.  It kind of seems like it's working, but it's reminded me about a related bug.  Whenever you disconnect from the client and then reconnect or whenever you reboot the server,  it says on the bottom of the screen that you are reconnected and it will either say listening  or tap mic to start, but when I tap the mic, nothing happens and when I talk, you don't  seem to hear me.

**[13:58:28] Claude**
I can see the issue. Look at the mic button click handler:

```
micBtn.onclick = async () => {
  if (!audioContext) {
    await setupMic();
  }
};
```

After a disconnect, `teardownAudio` sets `audioContext` to null and `micWasSetup` stays true. Then on reconnect, `ws.onopen` calls `setupMic()` automatically — but here's the problem. `setupMic` calls `startSession()` which adds the session glow class, but more importantly, if `setupMic` throws or the user taps the mic button while `setupMic` is already running from `onopen`, the button click handler sees `audioContext` is still null and tries to set up again. That could cause a double-setup conflict.

But the real bug is actually simpler. When the mic button is tapped and `audioContext` is already set (because `setupMic` ran successfully from `onopen`), the click handler does nothing — it only calls `setupMic` if `audioContext` is null. So after a reconnect, if auto-setup worked, the button tap is a no-op. That's fine. But if auto-setup failed silently, you're stuck.

Let me look more carefully at the reconnect flow.Okay, I see the problem. When the WebSocket drops, we call `teardownAudio` which destroys everything — the mic stream, the AudioContext, the MediaRecorder. Then on reconnect, `onopen` tries to call `setupMic` again, but on iOS, calling `getUserMedia` a second time after you've explicitly stopped the tracks can fail or behave unpredictably.

The real fix is simpler: the audio pipeline has nothing to do with the WebSocket connection. We shouldn't tear it down when the socket closes. Let me make that change now.Now on reconnect in `onopen`, we need to be smarter. If the mic is already set up and running, we don't need to call `setupMic` again — just re-enable the button and resume listening.There's one more thing — `resetTurnState` clears the `levelCheckInterval`, which stops the voice activity detection loop. We need `startListening` to bring it back. But `startListening` is now called from `onopen`, so that's covered. Let me just double-check `resetTurnState` doesn't do anything else that would break the audio pipeline.

Looking at the code, `resetTurnState` also sets `playbackAnalyser = null`. That's fine — it'll be recreated when the next response plays. And it clears `levelCheckInterval` which we restart via `startListening`. Looks good.

Let me verify the final state of the connect block looks right:That all looks correct. The key insight is: when the WebSocket drops, we leave the mic pipeline completely alone. When it reconnects, if the mic is already set up, we just call `startListening` to restart the voice activity detection loop. No more destroying and recreating the AudioContext.

This should fix the issue. Want me to restart the server so the updated JavaScript gets served?

---


<!-- consolidated -->

<!-- consolidated -->
