# Claude Voice Session
**Date**: 2026-05-10
**Time**: 22:31:35

---

**[22:32:09] You**
Your server just crashed. Can you shake the logs and tell me what happened?

**[22:32:09] Claude**
The log cuts off mid-session so I can't see the crash itself. Let me check if there's more recent output.Found it. The crash was a 400 error from the Claude API — the message was "Extra inputs are not permitted" on a tool result image URL. This happened right after you asked me to navigate to YouTube, which means when I took a browser screenshot, I tried to pass it to Claude using a URL format, but the API only accepts base64 encoded images in tool results, not URLs. I need to fix how screenshots get sent to Claude.Found it. The `browser_screenshot` function returns a dict with `"url"` and `"title"` fields mixed in alongside the image content block. When that gets sent to Claude as a tool result, the API rejects it because those extra fields aren't permitted in the image content block schema. The fix is to strip those extra fields before sending to Claude, or just not include them in the image dict.Fixed. The screenshot function was including extra `url` and `title` fields in the image content block, which the Claude API rejects. Stripped them out — now the dict is exactly what the API expects.

---


<!-- consolidated -->

<!-- consolidated -->
